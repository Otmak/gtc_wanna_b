# .
apiVersion: v1
kind: Service
metadata:
  name: zpeek-react-service
  labels:
    app: zpeek-react
spec:
  type: LoadBalancer
  selector:
    app: zpeek-react
  ports:
  - port: 80
    targetPort: 8080